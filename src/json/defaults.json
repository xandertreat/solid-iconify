{
	"ICONIFY_API": "api.iconify.design",
	"REQUEST_OPTIONS": {
		"method": "GET",
		"credentials": "omit",
		"redirect": "follow",
		"cache": "force-cache",
		"headers": {
			"Accept": "application/json"
		},
		"referrerPolicy": "no-referrer",
		"keepalive": true
	},
	"CACHE_SIZE": { "strategy": "unlimited" },
	"COLLECTION_SIZE": { "strategy": "grow", "initial": 16 },
	"SANITIZE": false
}
